import CONSTANTS from "../constants/constants";
const ROLES = CONSTANTS.USER.ROLES;
const CONFIG = {
    USERS: {
        SECTION_ACCESS: [ROLES.SUPERADMIN, ROLES.ADMIN],
        SHOW_OPTIONS: {
            ROLES: [ROLES.SUPERADMIN, ROLES.ADMIN],
            STATUS: [CONSTANTS.USER.STATUS.ACTIVE, CONSTANTS.USER.STATUS.SUSPENDED]
        },
        INVITE: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        },
        EDIT: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        },
        SUSPEND: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        },
        REACTIVATE: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        },
        DELETE: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        },
        REINVITE: {
            [ROLES.SUPERADMIN]: [ROLES.ADMIN, ROLES.REPORTER],
            [ROLES.ADMIN]: [ROLES.REPORTER]
        }
    },
    BRANDS: {
        SECTION_ACCESS: [ROLES.SUPERADMIN, ROLES.ADMIN, ROLES.REPORTER],
        SHOW_OPTIONS: {
            ROLES: [ROLES.SUPERADMIN, ROLES.ADMIN],
            STATUS: [CONSTANTS.BRAND.STATUS.VERIFIED, CONSTANTS.BRAND.STATUS.SUSPENDED]
        },
        CREATE: [ROLES.SUPERADMIN, ROLES.ADMIN, ROLES.REPORTER],
        EDIT: [ROLES.SUPERADMIN, ROLES.ADMIN],
        DELETE: [ROLES.SUPERADMIN, ROLES.ADMIN],
        SUSPEND: [ROLES.SUPERADMIN, ROLES.ADMIN],
        REACTIVATE: [ROLES.SUPERADMIN, ROLES.ADMIN]
    },
    CLAIMS: {
        SECTION_ACCESS: [ROLES.SUPERADMIN, ROLES.ADMIN, ROLES.REPORTER],
        RAISE: [ROLES.SUPERADMIN, ROLES.ADMIN, ROLES.REPORTER]
    },
    USERLIST: {

    },
    APPROVALLIST: {

    }
};

export default CONFIG;
