import React from 'react';
import PropTypes from 'prop-types';
import './Webform.component.scss';

const Webform = props => {
  return (
    <div className='c-Webform'>
      In Component Webform
    </div>
  );
};

Webform.propTypes = {

};

export default Webform;