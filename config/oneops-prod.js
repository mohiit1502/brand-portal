//
// This partial config will be used when procss.env.ONEOPS_ENVPROFILE === "prod"
//

module.exports = {
  services: {
    // Setting this to prod instructs your app to connect to production Pangea services.
    env: "prod"
  }
};
